kotlin.code.style=official
org.gradle.jvmargs=-Xmx512m
org.gradle.parallel=true

# Опционально: учётные данные Artifactory (если репозиторий требует авторизацию).
# Локально можно задать здесь; в CI — переменные ARTIFACTORY_USERNAME, ARTIFACTORY_PASSWORD.
# artifactoryUsername=
# artifactoryPassword=
